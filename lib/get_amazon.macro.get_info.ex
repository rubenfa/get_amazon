defmodule GetAmazon.GetInfo do
  @moduledoc """
  **This macro is working in progress**. I'm trying to provide a DSL to make petitions to Amazon API
  """

  defmacro amazon_get(xml) do
    1
  end
end
